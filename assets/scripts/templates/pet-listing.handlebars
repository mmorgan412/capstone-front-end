<div class="button-center">

{{#each pets as |pet|}}

<!-- go through pets array and do something on each one -->

          <button type="button" data-id="{{pet.id}}" class="numberCircle col-xs-3" data-toggle="tooltip" data-placement="top" title="{{pet.name}}">
                  {{ pet.letter }}
          </button>


{{/each}}

</div>
