

    <h2>{{pet.name}}</h2>

    <div class="col-xs-2" >
      <button data-id="{{pet.id}}" type="button" class="remove">Remove</button>
    </div>
    <div class="col-xs-2">
      <button data-id="{{pet.id}}"type="button" class="edit" data-toggle="modal" data-target="#edit-pet-modal">Update</button>
    </div>
